% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responder-cles.R
\name{responder_cles}
\alias{responder_cles}
\title{Common-language effect size (probabilistic index)}
\usage{
responder_cles(
  data,
  direction = c("higher", "lower"),
  pooling = c("fixed", "random"),
  tau_method = c("DL", "REML"),
  ci_method = c("wald", "hksj"),
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame with columns \code{study}, \code{change_e}, \code{sd_e}, \code{n_e},
\code{change_c}, \code{sd_c}, \code{n_c}. See \link{sample_responder_data}.}

\item{direction}{\code{"higher"} (a larger change is better) or \code{"lower"}.}

\item{pooling}{\code{"fixed"} (default) or \code{"random"} effects.}

\item{tau_method}{Between-study variance estimator for random effects:
\code{"DL"} (default) or \code{"REML"}.}

\item{ci_method}{Random-effects interval method: \code{"wald"} (default) or
\code{"hksj"} (Hartung-Knapp-Sidik-Jonkman, better for small numbers of studies).}

\item{conf_level}{Confidence level (default \code{0.95}).}
}
\value{
A list with:
\describe{
\item{studies}{Per-study data frame: \code{study}, \code{delta}, \code{cles}, \code{cles_lb},
\code{cles_ub}.}
\item{cles, cles_lb, cles_ub}{Pooled CLES and its interval.}
\item{delta, se_delta}{Pooled standardized difference and its SE.}
\item{tau2, i2, q, q_p, pi_lb, pi_ub}{Heterogeneity statistics; the
prediction interval is back-transformed to the CLES scale.}
\item{pooling, k}{Settings echoed back.}
}
}
\description{
A threshold-free responder-type measure: the probability that a randomly
chosen treated patient has a better change score than a randomly chosen
control. Under a Normal model this is exact,
\eqn{\mathrm{CLES} = \Phi(\delta)} with
\eqn{\delta = (\mu_e - \mu_c) / \sqrt{\sigma_e^2 + \sigma_c^2}}
(the sign is flipped when \code{direction = "lower"}). Per-study \eqn{\delta} values
are pooled by fixed- or random-effect inverse variance and back-transformed,
so no minimal important difference is required.
}
\examples{
cles <- responder_cles(sample_responder_data)
cles$cles
}
\references{
McGraw KO, Wong SP (1992). A common language effect size statistic.
\emph{Psychological Bulletin}, 111(2), 361 to 365.
}
